# Python algorithm for generating the fibonacci sequence

def recur_fibo(n):
   #for first two elements of the fibonacci sequence (0,1)
   if n <= 1:
       return n
   #will recursively add the two previous elements in the sequence to create the current element
   else:
       return(recur_fibo(n-1) + recur_fibo(n-2))

